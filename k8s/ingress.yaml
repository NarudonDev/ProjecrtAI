apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: projecrtai-web
  namespace: projecrtai
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: nginx
  rules:
    - host: projecrtai.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: projecrtai-web
                port:
                  number: 80
